{
  "mcpServers": {
    "mcp-server": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "--directory",
        "${PROJECT_DIR}",
        "run",
        "mcp-server"
      ],
      "env": {
        "MCP_SERVER_NAME": "mcp-server"
      }
    }
  }
}
